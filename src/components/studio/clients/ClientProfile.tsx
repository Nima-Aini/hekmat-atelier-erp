"use client";import { ExternalLink,FolderKanban,Phone,Wallet,X } from "lucide-react";import { formatMoney,toJalaliDate } from "@/lib/dateUtils";
export function ClientProfile({client,onClose,onOpenProject}:{client:any;onClose:()=>void;onOpenProject:(id:string)=>void}){return <div className="fixed inset-0 z-[100] flex items-end justify-center bg-black/75 sm:items-center sm:p-4"><section className="max-h-[96dvh] w-full overflow-y-auto rounded-t-3xl border border-slate-700 bg-slate-950 p-5 sm:max-w-5xl sm:rounded-3xl"><header className="flex items-start justify-between"><div><h2 className="text-xl font-black text-white">{client.name}</h2><p className="mt-1 flex items-center gap-2 text-sm text-slate-400"><Phone className="h-4 w-4"/>{client.mobile}</p></div><button onClick={onClose} className="rounded-xl border border-slate-700 p-2"><X className="h-5 w-5"/></button></header><div className="mt-5 grid grid-cols-2 gap-3 lg:grid-cols-5"><Metric label="تعداد پروژه" value={String(client.summary?.totalProjects||0)}/><Metric label="ارزش قرارداد" value={formatMoney(client.summary?.totalContracted||0)}/><Metric label="پرداخت‌شده" value={formatMoney(client.summary?.totalPaid||0)}/><Metric label="مانده" value={formatMoney(client.summary?.balance||0)}/><Metric label="رزرو بعدی" value={client.summary?.upcomingBooking?toJalaliDate(client.summary.upcomingBooking.eventDate):'—'}/></div><div className="mt-6 grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border border-slate-800 bg-slate-900/60 p-4"><h3 className="flex items-center gap-2 font-black"><FolderKanban className="h-5 w-5 text-cyan-400"/>پروژه‌ها</h3><div className="mt-3 space-y-2">{client.projects?.map((project:any)=><button key={project.id} onClick={()=>onOpenProject(project.id)} className="flex w-full justify-between rounded-xl bg-slate-950 p-3 text-right"><span><b className="text-white">{project.title}</b><small className="mt-1 block text-slate-400">{project.projectNumber} · {toJalaliDate(project.eventDate)}</small></span><span className="text-xs text-cyan-300">{project.status}</span></button>)}</div></section><section className="rounded-2xl border border-slate-800 bg-slate-900/60 p-4"><h3 className="flex items-center gap-2 font-black"><Wallet className="h-5 w-5 text-emerald-400"/>پرونده و ترجیحات</h3><dl className="mt-3 space-y-3 text-sm"><Row label="منبع آشنایی" value={client.referrer}/><Row label="نوع مشتری" value={client.customerType}/><Row label="شبکه اجتماعی" value={client.socialMedia}/><Row label="یادداشت" value={client.notes}/><Row label="نشانی" value={client.address}/></dl></section></div>{client.files?.length>0&&<section className="mt-5 rounded-2xl border border-slate-800 bg-slate-900/60 p-4"><h3 className="font-black">فایل‌ها و لینک‌های تحویل</h3><div className="mt-3 grid gap-2 md:grid-cols-2">{client.files.map((file:any)=><div key={file.id} className="rounded-xl bg-slate-950 p-3"><div className="flex justify-between"><b>{file.title}</b><span className="text-xs text-cyan-300">{file.status}</span></div>{file.url&&<a href={file.url} target="_blank" rel="noreferrer" className="mt-2 flex items-center gap-1 text-xs text-blue-300"><ExternalLink className="h-3.5 w-3.5"/>بازکردن لینک</a>}</div>)}</div></section>}<section className="mt-5 rounded-2xl border border-slate-800 bg-slate-900/60 p-4"><h3 className="font-black">تایم‌لاین ارتباط و پروژه</h3><div className="mt-3 space-y-2">{client.timeline?.slice(0,20).map((item:any)=><div key={item.id} className="border-r-2 border-cyan-500/40 pr-3"><b className="text-sm text-white">{item.title}</b><p className="text-xs text-slate-400">{toJalaliDate(item.createdAt,{showTime:true})} · {item.authorName}</p></div>)}</div></section></section></div>}
function Metric({label,value}:{label:string;value:string}){return <div className="rounded-xl border border-slate-800 bg-slate-900 p-3"><small className="text-slate-400">{label}</small><b className="mt-1 block text-sm text-white">{value}</b></div>}function Row({label,value}:{label:string;value?:string}){return <div className="flex justify-between gap-3"><dt className="text-slate-500">{label}</dt><dd className="text-left text-slate-200">{value||'—'}</dd></div>}
