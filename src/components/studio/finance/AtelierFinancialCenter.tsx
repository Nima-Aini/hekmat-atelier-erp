"use client";import { useState } from "react";import { FinancialView } from "@/components/views/FinancialView";import { InvoicesView } from "@/components/views/InvoicesView";import { TaxDeclarationView } from "@/components/views/TaxDeclarationView";
export function AtelierFinancialCenter({selectedProjectId}:{selectedProjectId:string|null}){const[tab,setTab]=useState<'cash'|'invoices'|'tax'>('cash');return <div className="space-y-4"><header><h1 className="text-2xl font-black">مرکز مالی آتلیه</h1><p className="text-sm text-slate-400">قراردادها، وصول، هزینه پروژه، نقدینگی و آماده‌سازی مالیاتی</p></header><div className="flex gap-2 overflow-x-auto">{[['cash','دریافت، هزینه و حساب‌ها'],['invoices','صورتحساب قراردادها'],['tax','آماده‌سازی مالیاتی']].map(([id,label])=><button key={id} onClick={()=>setTab(id as typeof tab)} className={`whitespace-nowrap rounded-xl px-4 py-2 text-sm font-bold ${tab===id?'bg-cyan-600':'border border-slate-700 text-slate-300'}`}>{label}</button>)}</div>{tab==='cash'?<FinancialView/>:tab==='invoices'?<InvoicesView selectedProjectId={selectedProjectId}/>:<TaxDeclarationView selectedProjectId={selectedProjectId}/>}</div>}
