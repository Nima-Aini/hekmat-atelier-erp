"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import { BanknoteArrowDown, RefreshCw, Search, WalletCards } from "lucide-react";
import { JalaliDatePicker } from "@/components/ui/JalaliDatePicker";
import { MoneyInput } from "@/components/ui/MoneyInput";
import { atelierToast } from "@/lib/atelierFeedback";
import { atelierLabel } from "@/lib/atelierLabels";
import { AtelierModal } from "./AtelierModal";
import { AccountSelector, NeonStatus, PaymentMethodSelect, type AccountOption } from "./FinanceControls";

type FinanceData = Record<string, any>;
const TABS = ["نمای کلی", "حساب‌ها و صندوق‌ها", "دریافت‌ها", "پرداخت‌ها", "هزینه‌ها", "مطالبات", "اقساط", "حقوق و دستمزد", "بدهی‌ها", "سود قراردادها", "گردش نقدینگی", "گزارش مالی"] as const;
const money = (value: unknown) => `${Number(value || 0).toLocaleString("fa-IR")} تومان`;
const date = (value: unknown, showTime = false) => value ? new Intl.DateTimeFormat("fa-IR-u-ca-persian", { dateStyle: "medium", ...(showTime ? { timeStyle: "short" } : {}), timeZone: "Asia/Tehran" }).format(new Date(String(value))) : "—";
const inputClass = "atelier-input w-full px-3 py-2.5 text-sm";

function Empty({ children = "داده‌ای برای نمایش وجود ندارد." }: { children?: string }) { return <div className="rounded-2xl border border-dashed border-zinc-800 p-8 text-center text-sm text-zinc-500">{children}</div>; }
function Table({ headers, rows }: { headers: string[]; rows: React.ReactNode[][] }) { return rows.length ? <div className="overflow-x-auto rounded-2xl border border-zinc-800"><table className="w-full min-w-[760px] text-right text-sm"><thead className="bg-zinc-950 text-xs text-zinc-500"><tr>{headers.map((h) => <th key={h} className="px-4 py-3 font-medium">{h}</th>)}</tr></thead><tbody className="divide-y divide-zinc-900">{rows.map((cells, i) => <tr key={i} className="bg-[#0b0b0d] hover:bg-red-950/10">{cells.map((cell, j) => <td key={j} className="px-4 py-3 text-zinc-300">{cell}</td>)}</tr>)}</tbody></table></div> : <Empty />; }
function Metric({ title, value, tone = "info" }: { title: string; value: unknown; tone?: "info" | "success" | "critical" | "warning" }) { return <div className="atelier-card p-4"><div className="flex items-center justify-between gap-2"><p className="text-xs text-zinc-500">{title}</p><NeonStatus tone={tone}>{tone === "success" ? "سالم" : tone === "critical" ? "بحرانی" : tone === "warning" ? "توجه" : "اطلاعات"}</NeonStatus></div><p className="mt-3 text-lg font-black">{money(value)}</p></div>; }
function Bars({ rows }: { rows: { label: string; amount: number }[] }) { const max = Math.max(1, ...rows.map((r) => Math.abs(r.amount))); return rows.length ? <div className="space-y-3">{rows.map((r) => <div key={r.label}><div className="mb-1 flex justify-between text-xs"><span>{r.label}</span><span>{money(r.amount)}</span></div><div className="h-2 rounded bg-zinc-900"><div className={`h-full rounded ${r.amount >= 0 ? "bg-emerald-500" : "bg-red-500"}`} style={{ width: `${Math.max(3, Math.abs(r.amount) / max * 100)}%` }} /></div></div>)}</div> : <Empty />; }
const statusTone = (status: string) => status === "paid" ? "success" : status === "overdue" ? "critical" : status === "due_soon" ? "warning" : "info";
const statusLabel = (status: string) => ({ paid: "پرداخت شده", partial: "بخشی پرداخت شده", overdue: "سررسید گذشته", due_soon: "سررسید نزدیک", pending: "در انتظار" }[status] || status);

export function AtelierFinanceView({ initialContractId }: { initialContractId?: string | null }) {
  const [tab, setTab] = useState<(typeof TABS)[number]>("نمای کلی"), [data, setData] = useState<FinanceData | null>(null), [busy, setBusy] = useState(false), [error, setError] = useState("");
  const [receipt, setReceipt] = useState<any>(null), [contract, setContract] = useState<any>(null), [schedule, setSchedule] = useState<any>(null), [adjust, setAdjust] = useState<any>(null), [outgoing, setOutgoing] = useState<any>(null);
  const [search, setSearch] = useState(""), [filter, setFilter] = useState("all"), [sort, setSort] = useState("nearest"), [expense, setExpense] = useState<any>({ paid: false, date: new Date() });
  const load = useCallback(async () => { setBusy(true); setError(""); try { const response = await fetch("/api/atelier/finance", { cache: "no-store" }); const body = await response.json(); if (!response.ok || !body.success) throw new Error(body.error || "دریافت اطلاعات مالی ناموفق بود."); setData(body.data); } catch (reason) { setError(reason instanceof Error ? reason.message : "خطای نامشخص"); } finally { setBusy(false); } }, []);
  useEffect(() => { void load(); }, [load]);
  useEffect(() => { if (initialContractId && data) { const row = data.contractFinance?.find((item: any) => item.id === initialContractId); if (row) { setTab("مطالبات"); setContract(row); } } }, [initialContractId, data]);
  const mutate = async (url: string, body: Record<string, unknown>, method = "POST") => { setBusy(true); setError(""); try { const response = await fetch(url, { method, headers: { "content-type": "application/json" }, body: JSON.stringify(body) }); const result = await response.json(); if (!response.ok || !result.success) throw new Error(result.error || "ثبت عملیات ناموفق بود."); await load(); return true; } catch (reason) { const message = reason instanceof Error ? reason.message : "خطای نامشخص"; setError(message); atelierToast(message, "error"); return false; } finally { setBusy(false); } };
  const accounts: AccountOption[] = data?.accounts || [], sources = data?.receivableSources || [], summary = data?.summary || {};
  const action = (label: string, click: () => void, secondary = false) => <button type="button" disabled={busy} onClick={click} className={secondary ? "atelier-button-secondary text-xs" : "atelier-button text-xs"}>{label}</button>;
  const installmentRows = useMemo(() => { const q = search.trim().toLocaleLowerCase("fa-IR"); return [...(data?.installments || [])].filter((r: any) => (!q || [r.customerName, r.customerMobile, r.contractNumber, r.title].some((v) => String(v || "").toLocaleLowerCase("fa-IR").includes(q))) && (filter === "all" || r.status === filter || (filter === "unpaid" && r.status !== "paid"))).sort((a: any, b: any) => sort === "highest" ? b.amount - a.amount : sort === "remaining" ? b.remainingAmount - a.remainingAmount : sort === "customer" ? String(a.customerName).localeCompare(String(b.customerName), "fa") : +new Date(a.dueDate) - +new Date(b.dueDate)); }, [data, search, filter, sort]);
  const actualFlow = useMemo(() => (data?.cashflow || []).map((r: any) => ({ label: r.period, amount: Number(r.actualIn) - Number(r.actualOut) })), [data]);

  const overview = <div className="space-y-5"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric title="نقدینگی" value={summary.liquidity} /><Metric title="دریافت این ماه" value={summary.receivedThisMonth} tone="success" /><Metric title="مطالبات" value={summary.receivable} tone={summary.receivable ? "warning" : "success"} /><Metric title="بدهی پرسنل" value={summary.personnelDebt} tone={summary.personnelDebt ? "critical" : "success"} /><Metric title="بدهی اجاره" value={summary.rentalDebt} tone={summary.rentalDebt ? "critical" : "success"} /><Metric title="جریان نقدی" value={summary.netCashflow} tone={summary.netCashflow >= 0 ? "success" : "critical"} /></div><section className="atelier-card p-5"><h2 className="mb-4 font-black">روند جریان نقدی</h2><Bars rows={actualFlow} /></section></div>;
  const accountView = <Table headers={["نام", "نوع", "موجودی", "وضعیت", "عملیات"]} rows={accounts.map((r) => [<div key="name"><strong>{r.name}</strong><p className="text-xs text-zinc-600">{r.bankName || "حساب آتلیه"}</p></div>, atelierLabel(r.type), money(r.balance), <NeonStatus key="s" tone="success">فعال</NeonStatus>, action("اصلاح موجودی", () => setAdjust(r), true)])} />;
  const receiptView = <div className="space-y-4"><div className="flex justify-end"><button onClick={() => setReceipt({ sourceType: "contract" })} className="atelier-button !bg-emerald-700"><BanknoteArrowDown className="h-4 w-4" />ثبت دریافت</button></div><Table headers={["تاریخ", "منبع", "حساب", "مبلغ", "روش"]} rows={(data?.receipts || []).map((r: any) => [date(r.paymentDate, true), r.source?.title || "دریافت", r.accountName, money(r.amount), atelierLabel(r.paymentMethod)])} /></div>;
  const contractView = <Table headers={["مشتری / قرارداد", "پروژه", "نهایی", "دریافت", "مانده", "عملیات"]} rows={(data?.contractFinance || []).map((r: any) => [<div key="c"><strong>{r.customerName}</strong><p className="text-xs text-zinc-600">{r.contractNumber} · {r.customerMobile}</p></div>, r.projectType || r.projectTitle, money(r.finalAmount), money(r.paidAmount), money(r.remainingAmount), <div key="a" className="flex gap-2">{action("پرونده مالی", () => setContract(r))}{r.remainingAmount > 0 && action("ثبت دریافت", () => setReceipt({ sourceType: "contract", sourceId: r.id }), true)}</div>])} />;
  const installmentView = <div className="space-y-4"><div className="atelier-card grid gap-3 p-4 md:grid-cols-3"><label className="relative"><Search className="absolute right-3 top-3 h-4 w-4 text-zinc-600" /><input className={`${inputClass} pr-9`} placeholder="مشتری، موبایل، قرارداد، عنوان" value={search} onChange={(e) => setSearch(e.target.value)} /></label><select className={inputClass} value={filter} onChange={(e) => setFilter(e.target.value)}><option value="all">همه</option><option value="unpaid">پرداخت‌نشده</option><option value="partial">بخشی پرداخت شده</option><option value="paid">پرداخت شده</option><option value="due_soon">سررسید نزدیک</option><option value="overdue">سررسید گذشته</option></select><select className={inputClass} value={sort} onChange={(e) => setSort(e.target.value)}><option value="nearest">نزدیک‌ترین سررسید</option><option value="highest">بیشترین مبلغ</option><option value="remaining">بیشترین مانده</option><option value="customer">نام مشتری</option></select></div><Table headers={["مشتری / قرارداد", "قسط", "مبلغ", "پرداخت‌شده", "مانده", "سررسید", "وضعیت", "عملیات"]} rows={installmentRows.map((r: any) => [<div key="c"><strong>{r.customerName}</strong><p className="text-xs text-zinc-600">{r.contractNumber} · {r.projectType || r.projectTitle}</p></div>, r.title, money(r.amount), money(r.paidAmount), money(r.remainingAmount), <div key="d">{date(r.dueDate)}<p className="text-xs text-zinc-600">برنامه: {date(r.programDate)}</p></div>, <NeonStatus key="s" tone={statusTone(r.status) as any}>{statusLabel(r.status)}</NeonStatus>, r.remainingAmount > 0 ? action("پرداخت قسط", () => setReceipt({ ...r, sourceType: "installment", sourceId: r.id })) : "—"])} /></div>;
  const expenseView = <div className="space-y-4"><form className="atelier-card grid gap-3 p-4 md:grid-cols-2 xl:grid-cols-3" onSubmit={async (e) => { e.preventDefault(); if (await mutate("/api/atelier/finance/expenses", { title: expense.title, category: expense.category || "general", amount: expense.amount, expenseDate: expense.date?.toISOString(), paid: expense.paid, accountId: expense.paid ? expense.accountId : null, idempotencyKey: crypto.randomUUID() })) setExpense({ paid: false, date: new Date() }); }}><label><span className="atelier-label">عنوان *</span><input required className={inputClass} value={expense.title || ""} onChange={(e) => setExpense({ ...expense, title: e.target.value })} /></label><div><span className="atelier-label">مبلغ *</span><MoneyInput value={expense.amount || 0} onChange={(amount) => setExpense({ ...expense, amount })} unit="تومان" /></div><JalaliDatePicker label="تاریخ" value={expense.date} onChange={(value) => setExpense({ ...expense, date: value })} /><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={expense.paid} onChange={(e) => setExpense({ ...expense, paid: e.target.checked })} />هم‌اکنون پرداخت شده</label>{expense.paid && <AccountSelector accounts={accounts} value={expense.accountId} onChange={(accountId) => setExpense({ ...expense, accountId })} label="حساب پرداخت" />}<div><button className="atelier-button">ثبت هزینه</button></div></form><Table headers={["تاریخ", "عنوان", "مبلغ", "پرداخت‌شده", "مانده", "عملیات"]} rows={(data?.expenses || []).map((r: any) => [date(r.expenseDate), r.title, money(r.amount), money(r.paidAmount), money(r.remainingAmount), r.remainingAmount > 0 ? action("پرداخت", () => setOutgoing({ kind: "expense", row: r })) : <NeonStatus key="s" tone="success">تسویه</NeonStatus>])} /></div>;
  const payments = <Table headers={["تاریخ", "شرح", "حساب", "مبلغ", "روش"]} rows={(data?.payments || []).map((r: any) => [date(r.paymentDate, true), r.notes || atelierLabel(r.paymentType), r.accountName, money(r.amount), atelierLabel(r.paymentMethod)])} />;
  const salaries = <Table headers={["پرسنل", "منبع", "مبلغ", "پرداخت‌شده", "مانده", "عملیات"]} rows={(data?.salaries || []).map((r: any) => [r.personnelName, `${r.sourceLabel} · ${r.sourceTitle}`, money(r.totalCalculated), money(r.paidAmount), money(r.remainingAmount), r.remainingAmount > 0 ? action("پرداخت دستمزد", () => setOutgoing({ kind: "personnel_wage", row: r })) : <NeonStatus key="s" tone="success">تسویه</NeonStatus>])} />;
  const payables = <Table headers={["عنوان", "دسته", "کل", "مانده", "عملیات"]} rows={(data?.payables || []).map((r: any) => [r.title, atelierLabel(r.category), money(r.amount), money(r.remainingAmount), action("پرداخت", () => setOutgoing({ kind: r.sourceType === "rental" ? "rental" : "expense", row: r }))])} />;
  const profitability = <Table headers={["قرارداد", "نوع", "درآمد", "هزینه", "سود", "وضعیت"]} rows={(data?.profitability || []).map((r: any) => [r.contractNumber, r.projectType || r.projectTitle, money(r.revenue), money(r.costs), money(r.profit), <NeonStatus key="s" tone={r.profit >= 0 ? "success" : "critical"}>{r.profit >= 0 ? "سود مثبت" : "زیان"}</NeonStatus>])} />;
  const reports = <div className="grid gap-4 lg:grid-cols-3">{[["درآمد بر اساس منبع", data?.reports?.incomeBySource], ["هزینه بر اساس دسته", data?.reports?.expensesByCategory], ["سود بر اساس نوع پروژه", data?.reports?.profitByProjectType]].map(([title, rows]) => <section className="atelier-card p-5" key={String(title)}><div className="mb-4 flex justify-between"><h2 className="font-black">{String(title)}</h2><NeonStatus tone={String(title).includes("سود") ? "success" : "info"}>تحلیل</NeonStatus></div><Bars rows={(rows as any[]) || []} /></section>)}</div>;
  const content: Record<string, React.ReactNode> = { "نمای کلی": overview, "حساب‌ها و صندوق‌ها": accountView, "دریافت‌ها": receiptView, "پرداخت‌ها": payments, "هزینه‌ها": expenseView, "مطالبات": contractView, "اقساط": installmentView, "حقوق و دستمزد": salaries, "بدهی‌ها": payables, "سود قراردادها": profitability, "گردش نقدینگی": <Bars rows={actualFlow} />, "گزارش مالی": reports };

  return <div className="space-y-5"><header className="flex items-center justify-between gap-3"><div><p className="atelier-kicker flex items-center gap-2"><WalletCards className="h-4 w-4" />مرکز مالی حکمت آتلیه</p><h1 className="mt-1 text-3xl font-black">مالی</h1><p className="mt-1 text-sm text-zinc-500">دریافت، اقساط، تعهدات و نقدینگی یکپارچه</p></div><button onClick={() => void load()} className="atelier-icon-button"><RefreshCw className={`h-5 w-5 ${busy ? "animate-spin" : ""}`} /></button></header>{error && <div role="alert" className="rounded-xl border border-red-800 bg-red-950/30 p-3 text-sm text-red-200">{error}</div>}<nav className="flex gap-2 overflow-x-auto pb-2">{TABS.map((name) => <button key={name} onClick={() => setTab(name)} className={`whitespace-nowrap rounded-xl border px-3 py-2 text-xs font-bold ${tab === name ? "border-red-600 bg-red-950/50 text-white shadow-[0_0_12px_rgba(239,68,68,.18)]" : "border-zinc-800 text-zinc-500"}`}>{name}</button>)}</nav>{content[tab]}
    {receipt && <ReceiptModal target={receipt} sources={sources} contracts={data?.contractFinance || []} accounts={accounts} busy={busy} onClose={() => setReceipt(null)} onSave={async (body: any) => { const url = receipt.sourceType === "installment" ? `/api/atelier/finance/installments/${receipt.sourceId}/payments` : "/api/atelier/finance/receipts"; if (await mutate(url, body)) { setReceipt(null); atelierToast("دریافت با موفقیت ثبت شد.", "success"); } }} />}
    {contract && <ContractModal contract={data?.contractFinance?.find((r: any) => r.id === contract.id) || contract} installments={(data?.installments || []).filter((r: any) => r.contractId === contract.id)} busy={busy} onClose={() => setContract(null)} onSchedule={() => { setSchedule(contract); setContract(null); }} onSave={async (body: any) => { if (await mutate(`/api/atelier/finance/contracts/${contract.id}`, body, "PUT")) { setContract(null); atelierToast("پرونده مالی اصلاح شد.", "success"); } }} />}
    {schedule && <ScheduleModal contract={schedule} initial={(data?.installments || []).filter((r: any) => r.contractId === schedule.id)} busy={busy} onClose={() => setSchedule(null)} onSave={async (rows: any[]) => { if (await mutate(`/api/atelier/finance/contracts/${schedule.id}/installments`, { installments: rows }, "PUT")) { setSchedule(null); atelierToast("برنامه اقساط ذخیره شد.", "success"); } }} />}
    {adjust && <AdjustmentModal account={adjust} busy={busy} onClose={() => setAdjust(null)} onSave={async (body: any) => { if (await mutate(`/api/atelier/finance/accounts/${adjust.id}/adjustments`, body)) { setAdjust(null); atelierToast("سند اصلاح موجودی ثبت شد.", "success"); } }} />}
    {outgoing && <OutgoingModal target={outgoing} accounts={accounts} busy={busy} onClose={() => setOutgoing(null)} onSave={async (body: any) => { const r = outgoing.row; const url = outgoing.kind === "personnel_wage" ? `/api/atelier/finance/obligations/personnel_wage/${r.id}` : outgoing.kind === "rental" ? `/api/atelier/finance/obligations/rental/${r.sourceId || r.id}` : `/api/atelier/finance/expenses/${r.id}/payments`; if (await mutate(url, body)) { setOutgoing(null); atelierToast("پرداخت ثبت شد.", "success"); } }} />}
  </div>;
}

function ReceiptModal({ target, sources, contracts, accounts, busy, onClose, onSave }: any) {
  const isInstallment = target.sourceType === "installment";
  const [sourceId, setSourceId] = useState(target.sourceId || ""), [amount, setAmount] = useState(Number(target.remainingAmount || 0)), [paidAt, setPaidAt] = useState<Date | null>(new Date()), [accountId, setAccountId] = useState(""), [method, setMethod] = useState("card_transfer"), [notes, setNotes] = useState("");
  const source = isInstallment ? target : sources.find((r: any) => r.sourceId === sourceId), contract = contracts.find((r: any) => r.id === (isInstallment ? target.contractId : sourceId));
  return <AtelierModal title={isInstallment ? "پرداخت قسط" : "ثبت دریافت"} onClose={onClose} wide><form onSubmit={(e) => { e.preventDefault(); onSave({ sourceType: isInstallment ? undefined : source?.sourceType, sourceId: isInstallment ? undefined : sourceId, accountId, amount, paidAt: paidAt?.toISOString(), paymentMethod: method, notes, idempotencyKey: crypto.randomUUID() }); }} className="space-y-5"><section className="rounded-2xl border border-cyan-900/60 bg-cyan-950/10 p-4"><h3 className="font-black">این دریافت متعلق به چیست؟</h3>{!isInstallment && !target.sourceId && <select required className={`${inputClass} mt-3`} value={sourceId} onChange={(e) => setSourceId(e.target.value)}><option value="">انتخاب قرارداد یا مراجعه</option>{sources.map((r: any) => <option key={`${r.sourceType}:${r.sourceId}`} value={r.sourceId}>{r.title} — مانده {money(r.remainingAmount)}</option>)}</select>}<div className="mt-3 grid gap-2 text-sm sm:grid-cols-2"><p><span className="text-zinc-500">مشتری:</span> {target.customerName || contract?.customerName || source?.title || "—"}</p><p><span className="text-zinc-500">قرارداد:</span> {target.contractNumber || contract?.contractNumber || "—"}</p>{contract && <><p>مبلغ قرارداد: {money(contract.finalAmount)}</p><p>مانده: {money(contract.remainingAmount)}</p></>}{isInstallment && <><p>قسط: {target.title}</p><p>سررسید: {date(target.dueDate)} · مانده {money(target.remainingAmount)}</p></>}</div></section><div className="grid gap-4 sm:grid-cols-2"><div><span className="atelier-label">مبلغ دریافت *</span><MoneyInput value={amount} onChange={setAmount} unit="تومان" /></div><JalaliDatePicker required label="تاریخ دریافت" value={paidAt} onChange={setPaidAt} /><AccountSelector accounts={accounts} value={accountId} onChange={setAccountId} label="حساب دریافت" /><PaymentMethodSelect value={method} onChange={setMethod} /><label className="sm:col-span-2"><span className="atelier-label">توضیحات</span><textarea className={`${inputClass} min-h-24`} value={notes} onChange={(e) => setNotes(e.target.value)} /></label></div>{amount > 0 && accountId && <div className="rounded-xl border border-emerald-900/60 bg-emerald-950/15 p-3 text-sm text-emerald-200">دریافت {money(amount)} به حساب {accounts.find((r: any) => r.id === accountId)?.name} در تاریخ {date(paidAt)} ثبت می‌شود.</div>}<div className="flex justify-end gap-2 border-t border-zinc-900 pt-4"><button type="button" onClick={onClose} className="atelier-button-secondary">انصراف</button><button disabled={busy || !source || !accountId || amount <= 0} className="atelier-button !bg-emerald-700">ثبت نهایی دریافت</button></div></form></AtelierModal>;
}

function ContractModal({ contract, installments, busy, onClose, onSchedule, onSave }: any) {
  const [discountAmount, setDiscountAmount] = useState(Number(contract.discountAmount || 0)), [financialNotes, setFinancialNotes] = useState(contract.financialNotes || ""); const final = Math.max(0, Number(contract.itemsSubtotal) - discountAmount);
  return <AtelierModal title={`پرونده مالی قرارداد ${contract.contractNumber}`} onClose={onClose} wide><form onSubmit={(e) => { e.preventDefault(); onSave({ discountAmount, financialNotes }); }} className="space-y-5"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{[["جمع آیتم‌ها", contract.itemsSubtotal], ["تخفیف ثابت", discountAmount], ["مبلغ نهایی", final], ["دریافت‌شده", contract.paidAmount], ["مانده", Math.max(0, final - contract.paidAmount)]].map(([label, value], i) => <div key={String(label)} className="rounded-xl border border-zinc-800 p-3"><p className="text-xs text-zinc-500">{label}</p><b className={`mt-2 block ${i === 3 ? "text-emerald-300" : i === 4 ? "text-amber-300" : ""}`}>{money(value)}</b></div>)}</div><section className="grid gap-4 rounded-2xl border border-zinc-800 p-4 sm:grid-cols-2"><div><span className="atelier-label">تخفیف ثابت</span><MoneyInput value={discountAmount} onChange={setDiscountAmount} unit="تومان" /></div><label><span className="atelier-label">یادداشت مالی</span><textarea className={`${inputClass} min-h-24`} value={financialNotes} onChange={(e) => setFinancialNotes(e.target.value)} /></label></section><section className="rounded-2xl border border-zinc-800 p-4"><h3 className="mb-3 font-black">تاریخچه دریافت‌ها</h3>{contract.receipts?.length ? <div className="space-y-2">{contract.receipts.map((r: any) => <div key={r.id} className="grid gap-2 rounded-xl bg-zinc-950 p-3 text-xs sm:grid-cols-4"><span>{date(r.paymentDate, true)}</span><b className="text-emerald-300">{money(r.amount)}</b><span>{r.accountName}</span><span>{atelierLabel(r.paymentMethod)}</span></div>)}</div> : <Empty>دریافتی ثبت نشده است.</Empty>}</section><section className="rounded-2xl border border-zinc-800 p-4"><div className="mb-3 flex items-center justify-between"><h3 className="font-black">اقساط</h3><button type="button" onClick={onSchedule} className="atelier-button-secondary">ویرایش برنامه اقساط</button></div>{installments.length ? <div className="space-y-2">{installments.map((r: any) => <div key={r.id} className="flex flex-wrap items-center justify-between gap-2 rounded-xl bg-zinc-950 p-3 text-xs"><span>{r.title} · {date(r.dueDate)}</span><span>{money(r.amount)} · مانده {money(r.remainingAmount)}</span><NeonStatus tone={statusTone(r.status) as any}>{statusLabel(r.status)}</NeonStatus></div>)}</div> : <Empty>برنامه اقساط ثبت نشده است.</Empty>}</section><section className="rounded-2xl border border-zinc-800 p-4"><h3 className="font-black">اصلاحات</h3><div className="mt-3 space-y-2 text-xs text-zinc-400">{contract.corrections?.map((r: any) => <p key={r.id}>{date(r.createdAt, true)} · {r.userName} · {r.action === "ATELIER_INSTALLMENTS_UPDATED" ? "اصلاح اقساط" : "اصلاح مبلغ قرارداد"}</p>)}{!contract.corrections?.length && <p>اصلاح مالی ثبت نشده است.</p>}</div></section><div className="flex justify-end gap-2"><button type="button" onClick={onClose} className="atelier-button-secondary">انصراف</button><button disabled={busy || final < contract.paidAmount} className="atelier-button">ذخیره اصلاح مالی</button></div></form></AtelierModal>;
}

function ScheduleModal({ contract, initial, busy, onClose, onSave }: any) {
  const [rows, setRows] = useState<any[]>(initial.length ? initial.map((r: any) => ({ id: r.id, title: r.title, amount: r.amount, dueDate: new Date(r.dueDate), paidAmount: r.paidAmount })) : [{ title: "بیعانه", amount: contract.remainingAmount, dueDate: new Date() }]); const update = (i: number, patch: any) => setRows((old) => old.map((r, x) => x === i ? { ...r, ...patch } : r));
  return <AtelierModal title={`برنامه اقساط — ${contract.contractNumber}`} onClose={onClose} wide><form onSubmit={(e) => { e.preventDefault(); onSave(rows.map((r) => ({ id: r.id, title: r.title, amount: r.amount, dueDate: r.dueDate?.toISOString() }))); }} className="space-y-4"><p className="text-sm text-zinc-400">هر قسط عنوان، مبلغ و تاریخ سررسید شمسی مستقل دارد. قسطی که دریافت دارد فقط تا مبلغی برابر یا بیشتر از دریافتی آن قابل اصلاح است.</p>{rows.map((r, i) => <div key={r.id || i} className="grid gap-3 rounded-2xl border border-zinc-800 p-4 sm:grid-cols-[1fr_1fr_1fr_auto]"><label><span className="atelier-label">عنوان *</span><input required className={inputClass} value={r.title} onChange={(e) => update(i, { title: e.target.value })} /></label><div><span className="atelier-label">مبلغ *</span><MoneyInput value={r.amount} onChange={(amount) => update(i, { amount })} unit="تومان" />{r.paidAmount > 0 && <small className="text-emerald-400">دریافت‌شده: {money(r.paidAmount)}</small>}</div><JalaliDatePicker required label="تاریخ سررسید" value={r.dueDate} onChange={(dueDate) => update(i, { dueDate })} /><button type="button" disabled={r.paidAmount > 0} onClick={() => setRows((old) => old.filter((_, x) => x !== i))} className="atelier-button-secondary self-end !text-red-300 disabled:opacity-40">حذف</button></div>)}<div className="flex flex-wrap items-center justify-between gap-3"><button type="button" onClick={() => setRows((old) => [...old, { title: `قسط ${old.length + 1}`, amount: 0, dueDate: new Date() }])} className="atelier-button-secondary">افزودن قسط</button><b>جمع: {money(rows.reduce((s, r) => s + Number(r.amount || 0), 0))}</b></div><div className="flex justify-end gap-2"><button type="button" onClick={onClose} className="atelier-button-secondary">انصراف</button><button disabled={busy || !rows.length || rows.some((r) => !r.dueDate || !r.title || r.amount <= 0)} className="atelier-button">ذخیره برنامه</button></div></form></AtelierModal>;
}

function AdjustmentModal({ account, busy, onClose, onSave }: any) {
  const [newBalance, setNewBalance] = useState(Number(account.balance)), [reason, setReason] = useState(""), [adjustedAt, setAdjustedAt] = useState<Date | null>(new Date()); const delta = newBalance - Number(account.balance);
  return <AtelierModal title={`اصلاح موجودی — ${account.name}`} onClose={onClose}><form onSubmit={(e) => { e.preventDefault(); onSave({ newBalance, reason, adjustedAt: adjustedAt?.toISOString(), idempotencyKey: crypto.randomUUID() }); }} className="space-y-5"><div className="grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-zinc-800 p-3"><p className="text-xs text-zinc-500">موجودی فعلی</p><b>{money(account.balance)}</b></div><div className={`rounded-xl border p-3 ${delta >= 0 ? "border-emerald-900 text-emerald-300" : "border-red-900 text-red-300"}`}><p className="text-xs opacity-70">مبلغ تعدیل</p><b>{delta >= 0 ? "+" : ""}{money(delta)}</b></div></div><div><span className="atelier-label">موجودی جدید *</span><MoneyInput value={newBalance} onChange={setNewBalance} unit="تومان" /></div><JalaliDatePicker required label="تاریخ اصلاح" value={adjustedAt} onChange={setAdjustedAt} /><label><span className="atelier-label">دلیل اصلاح *</span><textarea required minLength={5} className={`${inputClass} min-h-24`} value={reason} onChange={(e) => setReason(e.target.value)} /></label><div className="rounded-xl border border-amber-900/60 bg-amber-950/15 p-3 text-sm text-amber-200">موجودی از {money(account.balance)} به {money(newBalance)} تغییر می‌کند و اختلاف به‌عنوان سند تعدیل ثبت می‌شود.</div><div className="flex justify-end gap-2"><button type="button" onClick={onClose} className="atelier-button-secondary">انصراف</button><button disabled={busy || reason.trim().length < 5 || delta === 0} className="atelier-button">تأیید اصلاح موجودی</button></div></form></AtelierModal>;
}

function OutgoingModal({ target, accounts, busy, onClose, onSave }: any) {
  const [amount, setAmount] = useState(Number(target.row.remainingAmount || 0)), [accountId, setAccountId] = useState(""), [paymentDate, setPaymentDate] = useState<Date | null>(new Date()), [method, setMethod] = useState("bank_transfer"), [notes, setNotes] = useState("");
  return <AtelierModal title="ثبت پرداخت" onClose={onClose}><form onSubmit={(e) => { e.preventDefault(); onSave({ amount, accountId, paymentDate: paymentDate?.toISOString(), paymentMethod: method, notes, idempotencyKey: crypto.randomUUID() }); }} className="space-y-4"><div className="rounded-xl border border-cyan-900/60 p-3"><p className="text-xs text-zinc-500">پرداخت برای</p><b>{target.row.title || target.row.personnelName || target.row.sourceTitle}</b><p className="text-xs text-zinc-500">مانده: {money(target.row.remainingAmount)}</p></div><div><span className="atelier-label">مبلغ *</span><MoneyInput value={amount} onChange={setAmount} unit="تومان" /></div><JalaliDatePicker required label="تاریخ پرداخت" value={paymentDate} onChange={setPaymentDate} /><AccountSelector accounts={accounts} value={accountId} onChange={setAccountId} label="حساب پرداخت" /><PaymentMethodSelect value={method} onChange={setMethod} /><label><span className="atelier-label">توضیحات</span><textarea className={`${inputClass} min-h-20`} value={notes} onChange={(e) => setNotes(e.target.value)} /></label><div className="flex justify-end gap-2"><button type="button" onClick={onClose} className="atelier-button-secondary">انصراف</button><button disabled={busy || !accountId || amount <= 0} className="atelier-button">ثبت پرداخت</button></div></form></AtelierModal>;
}
